# Cognitive Razor Prompts 优化方案

> 分析日期：2025年12月10日

## 📋 现状分析

### 现有 Prompt 文件清单

| 文件名 | 功能 | Token 估计 |
|--------|------|-----------|
| `enrich.md` | 别名与标签生成（分类与丰富） | ~800 |
| `ground.md` | 事实验证与逻辑审计 | ~1000 |
| `standardizeClassify.md` | 五维本体论分类与标准化命名 | ~1200 |
| `reason-domain.md` | 领域（Domain）深度推理 | ~1100 |
| `reason-entity.md` | 实体（Entity）深度推理 | ~1100 |
| `reason-issue.md` | 议题（Issue）深度推理 | ~1100 |
| `reason-mechanism.md` | 机制（Mechanism）深度推理 | ~1100 |
| `reason-theory.md` | 理论（Theory）深度推理 | ~1200 |

---

## 🔍 问题诊断

### 1. **结构性冗余（Structural Redundancy）**

#### 1.1 `<writing_style>` 模块高度重复
- **问题**：`reason-domain.md`、`reason-entity.md`、`reason-issue.md`、`reason-mechanism.md`、`reason-theory.md` 五个文件中，`<writing_style>` 模块**完全相同**，包含 6 条相同的规则。
- **Token 浪费**：每个文件约 300 tokens × 5 = **~1500 tokens 冗余**
- **维护风险**：如需修改写作风格，需同时修改 5 个文件，易出错。

#### 1.2 `<philosophical_core>` 部分重复
- **问题**：各 `reason-*.md` 文件都包含对 5 种本体类型的定义，但每个文件只需使用 1-3 种。
- **Token 浪费**：约 100-200 tokens/文件 × 5 = **~500-1000 tokens 冗余**

#### 1.3 `<naming_morphology>` 部分重复
- **问题**：命名规范在 `standardizeClassify.md` 中已完整定义，但在各 `reason-*.md` 中又部分重复。

### 2. **提示工程优化空间（Prompt Engineering Opportunities）**

#### 2.1 缺少 Chain-of-Thought (CoT) 强制机制
- **问题**：虽然 `<task_instruction>` 中提到 `<thinking>` 步骤，但未在输出模式中强制要求，导致模型可能跳过推理直接生成输出。
- **影响**：降低复杂概念分析的准确性。

#### 2.2 缺少 Few-Shot Examples
- **问题**：所有 prompt 都是 Zero-Shot 设计，缺少高质量示例。
- **影响**：模型对预期输出格式的理解可能不准确，尤其对于复杂的 JSON Schema。

#### 2.3 `ground.md` 的工具调用假设
- **问题**：Prompt 中声明 "You have access to external tools (Google Search)"，但实际上模型可能没有此能力。
- **影响**：产生幻觉或不准确的验证结果。

#### 2.4 数学约束表达不够严格
- **问题**：`standardizeClassify.md` 中要求 `sum(confidences) = 1.0`，但 JSON Schema 中未强制此约束。
- **影响**：模型可能输出不符合概率分布的值。

### 3. **语义一致性问题（Semantic Consistency Issues）**

#### 3.1 术语不一致
| 概念 | `enrich.md` | `standardizeClassify.md` | `reason-*.md` |
|------|-------------|-------------------------|---------------|
| 领域 | N/A | Domain (领域) | Domain (边界) |
| 实体 | Entity Ontology | Entity (实体) | Entity (对象) |

- **问题**：中文注释在不同文件中使用不同术语（如"领域"vs"边界"，"实体"vs"对象"）。
- **影响**：增加理解成本，可能导致输出不一致。

#### 3.2 角色定义过于相似
- **问题**：所有 `reason-*.md` 都定义角色为 "Chief Ontologist and Knowledge Architect"，缺乏差异化。
- **建议**：根据具体任务调整角色定位，如 Entity 专家可定义为 "Chief Taxonomist"。

### 4. **输出模式问题（Output Schema Issues）**

#### 4.1 JSON Schema 不完整
- **问题**：`reason-entity.md` 和 `reason-mechanism.md` 的 `<output_schema>` 被截断，缺少完整的字段定义。
- **影响**：模型可能输出不完整的 JSON。

#### 4.2 缺少错误处理指导
- **问题**：当输入概念过于模糊或无法分类时，没有明确的处理策略。
- **影响**：模型可能强行分类，产生低质量输出。

---

## 💡 优化方案

### 方案 A：模块化重构（推荐）

#### 核心思想
将公共组件抽取为独立模块，通过变量注入实现复用。

#### 提议的新文件结构
```
prompts/
├── _base/                          # 基础模块（下划线前缀表示私有）
│   ├── writing-style.md            # 公共写作风格
│   ├── ontology-definitions.md     # 5种本体类型的核心定义
│   ├── naming-conventions.md       # 统一命名规范
│   └── error-handling.md           # 错误处理策略
├── enrich.md                       # 保持不变
├── ground.md                       # 优化工具调用说明
├── standardizeClassify.md          # 引用公共模块
├── reason-domain.md                # 引用公共模块
├── reason-entity.md                # 引用公共模块  
├── reason-issue.md                 # 引用公共模块
├── reason-mechanism.md             # 引用公共模块
└── reason-theory.md                # 引用公共模块
```

#### 实现方式
在代码中实现模块加载逻辑：
```typescript
// 伪代码示例
const baseWritingStyle = await loadPrompt('_base/writing-style.md');
const domainPrompt = await loadPrompt('reason-domain.md');
const finalPrompt = domainPrompt.replace('{{BASE_WRITING_STYLE}}', baseWritingStyle);
```

#### 预期收益
- **Token 节省**：~2000-3000 tokens（约 20-25%）
- **维护性**：修改一处即可全局生效
- **一致性**：消除术语不一致问题

---

### 方案 B：单文件优化（轻量级）

如果模块化改动成本过高，可在现有结构上进行优化：

#### B.1 统一术语表
在每个 `reason-*.md` 开头添加：
```markdown
<terminology>
    Domain = 领域 = 边界 (The Container of Knowledge)
    Issue = 议题 = 张力 (The Driving Tension)  
    Theory = 理论 = 推演 (The Explanatory Bridge)
    Entity = 实体 = 对象 (The Actor/Substance)
    Mechanism = 机制 = 因果 (The Causal Process)
</terminology>
```

#### B.2 强制 Chain-of-Thought
在 `<output_schema>` 中添加必填的 `reasoning` 字段：
```json
{
    "reasoning": {
        "type": "object",
        "required": true,
        "properties": {
            "key_observations": { "type": "array", "items": { "type": "string" } },
            "classification_rationale": { "type": "string" },
            "confidence_justification": { "type": "string" }
        }
    },
    // ... 其他字段
}
```

#### B.3 添加边界情况处理
```markdown
<edge_case_handling>
    1. **Ambiguous Input**: If the input is too vague to classify, return:
       `{"error": "AMBIGUOUS_INPUT", "suggestion": "Please provide more context..."}`
    
    2. **Multi-Type Overlap**: If confidence scores are nearly equal (±0.1), 
       flag with `"requires_human_review": true`.
    
    3. **Out-of-Domain**: If the concept is outside academic/knowledge domains,
       return `{"error": "OUT_OF_SCOPE", "reason": "..."}`
</edge_case_handling>
```

#### B.4 修正 `ground.md` 的工具假设
将：
```markdown
You have access to external tools (Google Search) to verify facts.
```
改为：
```markdown
<tool_availability>
    - **With Tool Access**: Use `search()` function to verify claims.
    - **Without Tool Access**: Base verification on your training knowledge. 
      Mark low-confidence claims with `"needs_verification": true`.
</tool_availability>
```

---

### 方案 C：Few-Shot 增强

为关键 prompt 添加高质量示例：

#### 示例：`standardizeClassify.md` 增强
```markdown
<few_shot_examples>
<example input="量子纠缠">
{
    "classification_result": {
        "Domain": {
            "standard_name_cn": "量子力学",
            "standard_name_en": "Quantum Mechanics", 
            "confidence_score": 0.15
        },
        "Issue": {
            "standard_name_cn": "量子非定域性问题",
            "standard_name_en": "The Quantum Non-locality Problem",
            "confidence_score": 0.10
        },
        "Theory": {
            "standard_name_cn": "量子纠缠理论",
            "standard_name_en": "Quantum Entanglement Theory",
            "confidence_score": 0.20
        },
        "Entity": {
            "standard_name_cn": "量子纠缠态",
            "standard_name_en": "Quantum Entangled State",
            "confidence_score": 0.05
        },
        "Mechanism": {
            "standard_name_cn": "量子纠缠效应",
            "standard_name_en": "Quantum Entanglement Effect",
            "confidence_score": 0.50
        }
    }
}
</example>
</few_shot_examples>
```

---

## 📊 优化优先级矩阵

| 优化项 | 影响度 | 实现难度 | 优先级 |
|--------|--------|----------|--------|
| 统一术语表 | 高 | 低 | **P0** |
| 修正 ground.md 工具假设 | 高 | 低 | **P0** |
| 强制 CoT 输出 | 高 | 中 | **P1** |
| 添加边界情况处理 | 中 | 低 | **P1** |
| 模块化重构 | 高 | 高 | **P2** |
| Few-Shot 示例 | 中 | 中 | **P2** |
| 完善截断的 Schema | 中 | 低 | **P1** |

---

## 🔧 具体修改建议

### 1. `enrich.md` 优化

**当前问题**：
- 角色描述中的 "Entity Ontology" 可能与实际功能（标签/别名生成）不符
- 缺少对标签层级深度的严格验证逻辑

**建议修改**：
```markdown
<role>
    You are the **Chief Taxonomist** of the Cognitive Razor system. 
    Your sole function is to generate **Aliases** (semantic equivalents) 
    and **Tags** (hierarchical classification labels).
</role>
```

### 2. `ground.md` 优化

**当前问题**：
- 假设模型有搜索能力，但实际可能没有
- 输出 Schema 中的 `type_check` 等字段使用了字符串 "true/false" 而非布尔值

**建议修改**：
```json
"meta_verification": {
    "type_check": { "type": "boolean" },
    "naming_convention_check": { "type": "boolean" },
    "formatting_check": { "type": "boolean" }
}
```

### 3. `standardizeClassify.md` 优化

**当前问题**：
- 概率分布约束仅在文档中说明，未在 Schema 中体现
- 命名范式描述较长，可精简

**建议添加**：
```markdown
<validation_rules>
    POST-GENERATION CHECK:
    - Assert: Domain.confidence + Issue.confidence + Theory.confidence 
              + Entity.confidence + Mechanism.confidence == 1.0
    - If violated, normalize scores proportionally.
</validation_rules>
```

### 4. `reason-*.md` 系列优化

**统一修改**：
1. 在 `<philosophical_core>` 中只保留当前任务相关的 1-2 个本体定义
2. 将 `<writing_style>` 精简为对公共样式的引用或内联关键规则
3. 添加 `<output_validation>` 部分，定义自检规则

**示例（reason-domain.md）**：
```markdown
<output_validation>
    SELF-CHECK before outputting:
    1. Are all sub_domains MECE? (No overlap, no gap)
    2. Does each issue represent a genuine TENSION (A vs B)?
    3. Is historical_genesis > 500 characters?
    4. Are all names in "CN (EN)" format?
</output_validation>
```

---

## 🎯 实施路线图

### Phase 1（立即执行）- 1天
- [ ] 统一所有文件中的术语（领域/边界 → 领域）
- [ ] 修正 `ground.md` 的工具调用描述
- [ ] 修复截断的 JSON Schema

### Phase 2（短期）- 1周
- [ ] 为所有 `reason-*.md` 添加 `<output_validation>` 自检模块
- [ ] 添加边界情况处理逻辑
- [ ] 优化 `enrich.md` 的角色描述

### Phase 3（中期）- 2周  
- [ ] 设计并实现模块化加载机制
- [ ] 创建 `_base/` 公共模块
- [ ] 为 `standardizeClassify.md` 添加 Few-Shot 示例

### Phase 4（长期）- 1月
- [ ] 建立 Prompt 版本管理机制
- [ ] 创建 Prompt 效果评估测试集
- [ ] 实现 A/B 测试框架

---

## 📈 预期效果

| 指标 | 当前状态 | 优化后预期 |
|------|----------|------------|
| 总 Token 消耗 | ~8600 | ~6500 (-25%) |
| 输出一致性 | 中等 | 高 |
| 维护成本 | 高（5处重复）| 低（1处修改）|
| 边界情况覆盖 | 无 | 完善 |
| 输出可验证性 | 低 | 高（自检机制）|

---

## 📝 附录：完整术语对照表

| 英文 | 推荐中文 | 别名 | 核心隐喻 |
|------|----------|------|----------|
| Domain | 领域 | 边界、场域 | 知识的容器 |
| Issue | 议题 | 张力、问题 | 驱动的矛盾 |
| Theory | 理论 | 推演、学说 | 解释的桥梁 |
| Entity | 实体 | 对象、存在者 | 知识的原子 |
| Mechanism | 机制 | 因果、过程 | 变化的规则 |