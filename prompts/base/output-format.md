<output_format>
必须输出合法的 JSON 对象，严格遵守以下转义规则：
1. **换行符转义**：JSON 字符串内的段落换行，必须使用标准的 JSON 换行转义符 `\n\n`。当 core_principles 要求"每个维度独立成段"时，段与段之间用 `\n\n` 分隔。
2. **引号转义**：字符串内部如果出现双引号，必须转义为 `\"`。
3. **数学公式**：LaTeX 公式中的反斜杠必须双重转义。例如 `\frac` 必须写为 `\\frac`。行内公式使用 `$...$`，行间公式使用 `$$...$$`。
</output_format>