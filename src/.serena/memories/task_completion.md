# 任务完成前检查
- 运行 `npm run build` 确保插件可打包；如改动 TS/逻辑请运行相关单测（若有）或手动验证核心流程。
- 运行 `npx eslint ./src/`，必要时 `--fix`。
- 确认未提交的生成文件（main.js、node_modules 等）未被误改。
- 若改动配置/接口，更新对应类型定义和默认设置。
- 如新增命令/视图/设置，确保在入口注册并清理卸载资源（register*）。
- 文档/注释保持中文，命令/代码保持英文。